{
  "name": "naturallog",
  "productName": "Natural-Log",
  "version": "0.1.0",
  "main": "index.js",
  "dependencies": {
    "electron-log": "1.3.0",
    "express": "4.15.0",
    "nodejs-websocket": "1.7.1",
    "nodemon": "^2.0.19",
    "socket.io": "1.7.3"
  },
  "devDependencies": {
    "electron": "1.6.1",
    "electron-packager": "^15.5.1"
  },
  "scripts": {
    "dev": "./node_modules/.bin/nodemon -x 'npm run start'",
    "start": "./node_modules/.bin/electron .",
    "package-mac": "npx electron-packager . 'ln' --overwrite --platform=darwin --arch=x64 --icon=assets/icons/ico/icon.icns --prune=true --out=release-builds"
  }
}
